#include"partialSolution.hpp"

bool processOfEliminationbyCells(partialSolution & sol);
bool processOfEliminationbyBlocks(partialSolution & sol);
bool processOfEliminationbyRows(partialSolution & sol);
bool processOfEliminationbyColumns(partialSolution & sol);

bool rowEliminationbyBlocks(partialSolution & sol);
bool blockEliminationbyRows(partialSolution & sol);
bool columnEliminationbyBlocks(partialSolution & sol);
bool blockEliminationbyColumns(partialSolution & sol);
